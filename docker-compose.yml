version: '3'
services:
  tf:
    build: .
    stdin_open: true
    tty: true
    # Comment thie line below if needed
    runtime: nvidia
    volumes:
      - ./wheels:/wheels
      - ./binaries:/binaries
    environment:
        - TF_VERSION_GIT_TAG=v1.8.0
        - TF_COMPILATION_WITH_GPU=1
